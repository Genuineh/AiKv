[package]
name = "aikv"
version = "0.10.5"
edition = "2021"
description = "Redis RESP protocol compatible KV server based on aidb"
license = "MIT"

[[bin]]
name = "aikv"
path = "src/main.rs"

[features]
default = ["compression"]
monitoring = ["dep:tracing-opentelemetry", "dep:opentelemetry-otlp", "dep:opentelemetry", "dep:opentelemetry_sdk", "dep:hyper", "dep:hyper-util", "dep:http-body-util", "aidb/monitoring"]
cluster = ["aidb/cluster", "dep:openraft", "dep:openraft-rt-tokio"]
cluster-test-util = ["cluster", "aidb/cluster-test-util"]
compression = ["aidb/compression"]

[dependencies]
# Async runtime
tokio       = { version = "1", features = ["full"] }
tokio-util  = { version = "0.7", features = ["rt"] }
async-trait = "0.1"

# Core primitives
parking_lot = "0.12"
dashmap = "6"
bytes     = "1"
thiserror = "2"
serde     = { version = "1", features = ["derive"] }
serde_json = "1"
toml       = "0.8"
postcard  = { version = "1", default-features = false, features = ["alloc"] }
sha1      = "0.10"
itoa = "1"
ryu = "1"
crc32fast = "1"
rand      = "0.8"

# Allocator
mimalloc = "0.1"

# Logging & tracing
tracing            = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# CLI
clap = { version = "4", features = ["derive"] }

# Lua scripting
mlua = { version = "0.10", features = ["lua54", "async", "send", "vendored"] }

# Openraft
openraft = { version = "0.10.0-alpha.33", optional = true }
openraft-rt-tokio = { version = "0.10.0-alpha.33", optional = true }

# AiDb storage engine
# 开发期用 git 依赖 (可被任何人 clone 编译); 本地通过 ~/.cargo/config.toml 的
# [patch] 覆盖为 ../aidb 的 path, 保持改 aidb 立即可被 aikv 验证.
# 见 README.md §与 AiDb 与 CONTRIBUTING.md §aidb 依赖.
aidb = { git = "https://github.com/wiqun/AiDb.git", branch = "new/main" }

# Monitoring (optional)
# Metrics HTTP server (monitoring feature)
hyper = { version = "1", features = ["server", "http1"], optional = true }
hyper-util = { version = "0.1", features = ["tokio"], optional = true }
http-body-util = { version = "0.1", optional = true }
opentelemetry-otlp = { version = "0.32", features = ["grpc-tonic", "metrics"], optional = true }
opentelemetry = { version = "0.32", optional = true }
opentelemetry_sdk = { version = "0.32", features = ["rt-tokio", "testing"], optional = true }
tracing-opentelemetry = { version = "0.33", optional = true }

[dev-dependencies]
redis    = { version = "0.24", features = ["tokio-comp"] }
tempfile = "3"
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"
proptest = "1"

[profile.release]
lto           = true
codegen-units = 1
debug         = 1   # line tables for eBPF/Pyroscope flame graphs (不 strip)

[lints.clippy]
all = "warn"

